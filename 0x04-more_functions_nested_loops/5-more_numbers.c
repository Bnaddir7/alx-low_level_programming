#include "main.h"

/**
 * more_numbers - prints 10 times numbers 0-14
 */
void more_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		int n = 0;

		do {
			_putchar(n + 48);
			n++;
		} while (n = 0 && n < 10);
		_putchar('\n');
	}
}
